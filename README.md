# Sales Report
This file is a Sales data which comprises of 14 columns which is as follows: OrderID, Date, CustomerID, Products, Quantity, UnitPrice, ShippingAddress, PaymentMethod, OrderStatus, TrackingNumber, ItemsinCart, CouponCode, ReferralSource and TotalPrice.

For Task 1 which is this repository, its a fondation phase: Data cleaning and preparation. Before we build a single predictive model we must first of all clean the messy datasets into reliable sources of truth.
By completing this milestone, we would be able to identify missing values, eliminate duplicates, and correct formating through pure analytical logic.

### Data Cleaning and Preparation.
#### Key Steps Taken:
1. Identify Missing Values:
There are two method of handling missing data which is imputation and to delete. When it is a categorical data like we have in this dataset i.e CouponCode column we input the mode that is the most occuring vaalue in the column but for numerical dataset we input the median.
Consequently, in column CouponCode, the missing data shall be filled with the mode which is Freeship.
3. Correct data format(dates, numbers, text)
Here all columns format are properly formatted to the actaul context of the data contained in the column.

#### Keys Skills
Data cleaning, Excel or Python basics, data preparation
